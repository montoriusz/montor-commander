const CLASS_PREFIX = 'sctm';

/** data attribute = `data-term-block` / `dataset.termBlock` */

export const DATA_ATTR_TERM_BLOCK = 'data-term-block';
export const DATASET_TERM_BLOCK = 'termBlock';

export const CLASS_CHAT_MESSAGE = `${CLASS_PREFIX}-chat-msg`;
export const CLASS_CMD_SUGGESTION = `${CLASS_PREFIX}-cmd-sugg`;
export const CLASS_TERM_BLOCK = `${CLASS_PREFIX}-term-block`;
export const CLASS_TERM_CMD = `${CLASS_PREFIX}-term-cmd`;
